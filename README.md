# langtool

personal language study tool.
should build correctly using gnome builder or flatpak-builder with all needed sdks installed.

may crash randomly if ollama is not running, will hopefully be fixed later
